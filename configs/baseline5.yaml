
# ========================================
# Baseline 5 Configuration
# ========================================

experiment:
  name: baseline5_resnet50

train:
  batch_size: 32
  epochs: 15
  lr: .0001
  weight_decay: .0001
  eta_min: .000001
  drop_p: .5
  gamma: 1.0
  hidden_dim: 512
  lstm_num_layer : 1

  print_PerClass: False 


model:
  freeze_backbone: False

output:
  root: /nfs/slurm/assu002/projects/volleyball_project/outputs/baseline5
 
 